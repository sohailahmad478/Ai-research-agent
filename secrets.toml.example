# Copy this file to ".streamlit/secrets.toml" for LOCAL use and paste your real key.
# (secrets.toml is in .gitignore, so it will not be uploaded to GitHub.)
# On Streamlit Cloud, paste the same line into: App settings -> Secrets
GROQ_API_KEY = "gsk_your_key_here"
